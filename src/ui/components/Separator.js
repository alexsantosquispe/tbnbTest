import React from "react"
import { View } from "react-native"
import { GlobalStyles } from "../styles"

const Separator = () => {
  return <View style={GlobalStyles.separatorLine} />
}

export default Separator
